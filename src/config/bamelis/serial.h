/*
 * Bamelis fork configuration (serial.h overrides)
 *
 * Applied after config/serial.h via NAMED_CONFIG.  No overrides; the file
 * must exist because every config header includes its named variant.
 */
