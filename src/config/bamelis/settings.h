/*
 * Bamelis fork configuration (settings.h overrides)
 *
 * Applied after config/settings.h via NAMED_CONFIG.  No include guard
 * (see config/bamelis/general.h).
 */

/* Memory map settings (enables ${memsize} for RAM guards in menus) */
#undef  MEMMAP_SETTINGS
#define MEMMAP_SETTINGS
