/*
 * Bamelis fork configuration (crypto.h overrides)
 *
 * Applied after config/crypto.h via NAMED_CONFIG.  No overrides; the file
 * must exist because every config header includes its named variant.
 */
