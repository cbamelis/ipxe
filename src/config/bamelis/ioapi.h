/*
 * Bamelis fork configuration (ioapi.h overrides)
 *
 * Applied after config/ioapi.h via NAMED_CONFIG.  No overrides; the file
 * must exist because every config header includes its named variant.
 */
