/*
 * Bamelis fork configuration (sideband.h overrides)
 *
 * Applied after config/sideband.h via NAMED_CONFIG.  No overrides; the file
 * must exist because every config header includes its named variant.
 */
