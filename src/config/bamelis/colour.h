/*
 * Bamelis fork configuration (colour.h overrides)
 *
 * Applied after config/colour.h via NAMED_CONFIG.  No overrides; the file
 * must exist because every config header includes its named variant.
 */
